# Lower bounds only — let pip pick wheels that match the local Python (3.14 here).
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
sqlmodel>=0.0.22
psycopg[binary]>=3.2
python-dotenv>=1.0
httpx>=0.28
pytest>=8.3

# Phase 4 — LLM layer (behind app/llm.py). Fallback-first: no key => templated prose.
anthropic>=1.3,<2
