import { PageHeader, Placeholder } from "@/components/ui";

export default function CompliancePage() {
  return (
    <div>
      <PageHeader title="Compliance" subtitle="Deferred, suppressed, stopped and escalated — the system refusing to spam." />
      <Placeholder phase="7" />
    </div>
  );
}
